{
  "name": "@bdt/cli",
  "private": true,
  "version": "0.1.0",
  "description": "The `bdt` CLI entry point.",
  "main": "dist/cli.js",
  "bin": {
    "bdt": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@bdt/core": "*",
    "@bdt/native-input": "*",
    "@bdt/scenarios": "*",
    "@bdt/bds-adapter": "*",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.0",
    "@types/node": "^22.10.0"
  }
}
