{
  "name": "@bdt/core",
  "private": true,
  "version": "0.1.0",
  "description": "Shared types for bedrock-tools-v5. Zero runtime deps.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.0"
  }
}
